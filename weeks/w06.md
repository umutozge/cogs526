## Session 06

* Clarify and relate `consistency`, `contingency`, `contradiction`, `tautology`,`implication`, `validity`, `conditional`, `equivalence`, `biconditional`.
* Truth trees:
	* Apply non-branching before branching rules.

* Construct truth trees for the following:
    * $(p\to q) \to (p\to q\lor r)$
    * $(p\to q) \to (p\to q\land r)$
    * $(p\land s) \leftrightarrow (q \lor r)$
    * $((p\to q) \to s)\land (p\to (q\to s))$
    * $p\land (-q\lor -p)$
    * $((p\to -q) \to -p) \to q$
    * $(p\to q) \to (-p \to -q)$
    * $(p\to q) \to (-q \to -p)$
    * $((p\to q)\to p) \to p$
    * $(p\lor q) \to (p\to q)$
    * $(p\to q) \land (q\to r) \land (r\to p)$
    * $(p\to q) \lor (q\to r) \lor (r\to p)$
    * $-(-(-p\land -q)\land -p)\leftrightarrow -(q\lor -p)$


* Pick the formulas that imply and that are implied by $p\to q$.

$$
\begin{array}{ccc}
\neg p  &  q & \neg q \to \neg p \\
\neg p \lor q &  \neg (p \land \neg q) & (p\lor r) \to q\\
p \to (q \land r)  & p \to (q \lor r) & q \land r\\
(p\to q) \lor r &   (p \land r) \to q  &
\end{array}
$$

* Determine whether $p\leftrightarrow (q\leftrightarrow r)$ implies $r\leftrightarrow(q\to p)$, or vice versa.
* Likewise for $(p\leftrightarrow q\land r)\to p\land q$ and $(p\to q\land r)\leftrightarrow p\land q$
* Determine which of the four imply $q$ and which imply $\neg p$:

$$
\begin{array}{cccc}
p\land (p\to q) & \neg q \land(p \to q) & p\land (\neg p \to q) & p\to(\neg q \land q)
\end{array}
$$

* Determine what implications hold between these:

$$
\begin{array}{ccc}
p\to q & p\lor q \to r & p \to q\lor r
\end{array}
$$

